[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aps"
version = "0.1.0"
description = "APS1052 — BTC hourly extreme-movement prediction and trading validation"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
